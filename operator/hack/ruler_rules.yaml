groups:
  - name: PlatformRules
    interval: 1m
    rules:
    - record: logs:default:platform:rate1m
      expr: |
        sum(rate({namespace=~"default+"}[1m]))
      labels:
        metric: "Jacob"
        source: "logs"