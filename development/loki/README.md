# Grafana Loki

This sets up Grafana Loki through [docker-compose](https://docs.docker.com/compose/).

## Usage

### Spin up

Build it first:

```console
$ docker-compose build
```

Run it

```console
$ docker-compose up -d
```