schema_config:
  configs:
    - from: "2024-01-01"
      store: tsdb
      object_store: gcs
      schema: v13
      index:
        prefix: loki_index_
        period: 24h

storage_config:
  gcs:
    bucket_name: dev-us-central-0-loki-dev-005-data
    # service_account: ...   # optional: uses GOOGLE_APPLICATION_CREDENTIALS / default credentials if unset
