name: Checks
on: [push]
jobs:
  checks:
    runs-on: ubuntu-latest
    container:
      image: grafana/loki-build-image:0.32.0
    steps:
      - uses: actions/checkout@v4
      - name: Lint
        run: make lint
