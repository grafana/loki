syntax = "proto3";

package logproto;

import "wiresmith/options.proto";
import "google/protobuf/timestamp.proto";
import "pkg/logqlmodel/stats/stats.proto";
import "pkg/push/push.proto";
import "pkg/storage/chunk/cache/resultscache/types.proto";

option go_package = "github.com/grafana/loki/v3/pkg/logproto";

// Drop the per-message presence bitmap: gogoproto nullable=false parity
// (see WIRESMITH_MIGRATION.md).
option (wiresmith.options.no_presence_all) = true;

service Querier {
  rpc Query(QueryRequest) returns (stream QueryResponse) {}

  rpc QuerySample(SampleQueryRequest) returns (stream SampleQueryResponse) {}

  rpc Label(LabelRequest) returns (LabelResponse) {}

  rpc Tail(TailRequest) returns (stream TailResponse) {}

  rpc Series(SeriesRequest) returns (SeriesResponse) {}

  rpc TailersCount(TailersCountRequest) returns (TailersCountResponse) {}

  rpc GetChunkIDs(GetChunkIDsRequest) returns (GetChunkIDsResponse) {}

  // Note: this MUST be the same as the variant defined in
  // indexgateway.proto on the IndexGateway service.
  rpc GetStats(IndexStatsRequest) returns (IndexStatsResponse) {}
  // Note: this MUST be the same as the variant defined in
  // indexgateway.proto on the IndexGateway service.
  rpc GetVolume(VolumeRequest) returns (VolumeResponse) {}

  rpc GetDetectedFields(DetectedFieldsRequest) returns (DetectedFieldsResponse) {}

  rpc GetDetectedLabels(DetectedLabelsRequest) returns (LabelToValuesResponse) {}
}

message LabelToValuesResponse {
  map<string, UniqueLabelValues> labels = 1;
}

message UniqueLabelValues {
  repeated string values = 1;
}

service StreamData {
  rpc GetStreamRates(StreamRatesRequest) returns (StreamRatesResponse) {}
}

message StreamRatesRequest {}

message StreamRatesResponse {
  repeated StreamRate streamRates = 1 [(wiresmith.options.pointer) = true];
}

message StreamRate {
  uint64 streamHash = 1;
  uint64 streamHashNoShard = 2;
  int64 rate = 3; // rate in plain bytes.
  string tenant = 4;
  uint32 pushes = 5;
}

message QueryRequest {
  string selector = 1 [deprecated = true];
  uint32 limit = 2;
  google.protobuf.Timestamp start = 3 [(wiresmith.options.stdtime) = true];
  google.protobuf.Timestamp end = 4 [(wiresmith.options.stdtime) = true];
  Direction direction = 5;
  reserved 6;
  repeated string shards = 7 [(wiresmith.options.jsontag) = "shards,omitempty"];
  repeated Delete deletes = 8 [(wiresmith.options.pointer) = true];
  Plan plan = 9 [(wiresmith.options.customtype) = "github.com/grafana/loki/v3/pkg/querier/plan.QueryPlan"];
  // If populated, these represent the chunk references that the querier should
  // use to fetch the data, plus any other chunks reported by ingesters.
  ChunkRefGroup storeChunks = 10 [
    (wiresmith.options.pointer) = true,
    (wiresmith.options.jsontag) = "storeChunks"];
}

message SampleQueryRequest {
  string selector = 1 [deprecated = true]; // mark as reserved once we've fully migrated to plan.
  google.protobuf.Timestamp start = 2 [(wiresmith.options.stdtime) = true];
  google.protobuf.Timestamp end = 3 [(wiresmith.options.stdtime) = true];
  repeated string shards = 4 [(wiresmith.options.jsontag) = "shards,omitempty"];
  repeated Delete deletes = 5 [(wiresmith.options.pointer) = true];
  Plan plan = 6 [(wiresmith.options.customtype) = "github.com/grafana/loki/v3/pkg/querier/plan.QueryPlan"];
  // If populated, these represent the chunk references that the querier should
  // use to fetch the data, plus any other chunks reported by ingesters.
  ChunkRefGroup storeChunks = 10 [
    (wiresmith.options.pointer) = true,
    (wiresmith.options.jsontag) = "storeChunks"];
}

// TODO(owen-d): fix. This will break rollouts as soon as the internal repr is changed.
message Plan {
  bytes raw = 1;
}

message Delete {
  string selector = 1;
  int64 start = 2;
  int64 end = 3;
}

message QueryResponse {
  repeated StreamAdapter streams = 1 [
    (wiresmith.options.customtype) = "github.com/grafana/loki/pkg/push.Stream"
  ];
  stats.Ingester stats = 2;
  repeated string warnings = 3;
}

message SampleQueryResponse {
  repeated Series series = 1;
  stats.Ingester stats = 2;
  repeated string warnings = 3;
}

enum Direction {
  FORWARD = 0;
  BACKWARD = 1;
}

message LabelRequest {
  string name = 1;
  bool values = 2; // True to fetch label values, false for fetch labels names.
  google.protobuf.Timestamp start = 3 [(wiresmith.options.stdtime) = true];
  google.protobuf.Timestamp end = 4 [(wiresmith.options.stdtime) = true];
  string query = 5; // Naming this query instead of match because this should be with queryrangebase.Request interface
}

message LabelResponse {
  repeated string values = 1;
}

message Sample {
  int64 timestamp = 1 [(wiresmith.options.jsontag) = "ts"];
  double value = 2 [(wiresmith.options.jsontag) = "value"];
  uint64 hash = 3 [(wiresmith.options.jsontag) = "hash"];
}

// LegacySample exists for backwards compatibility reasons and is deprecated. Do not use.
message LegacySample {
  double value = 1;
  int64 timestamp_ms = 2;
}

message Series {
  string labels = 1 [(wiresmith.options.jsontag) = "labels"];
  repeated Sample samples = 2 [
    (wiresmith.options.jsontag) = "samples"
  ];
  uint64 streamHash = 3 [(wiresmith.options.jsontag) = "streamHash"];
}

message TailRequest {
  string query = 1 [deprecated = true];
  reserved 2;
  uint32 delayFor = 3;
  uint32 limit = 4;
  google.protobuf.Timestamp start = 5 [(wiresmith.options.stdtime) = true];
  Plan plan = 6 [(wiresmith.options.customtype) = "github.com/grafana/loki/v3/pkg/querier/plan.QueryPlan"];
}

message TailResponse {
  StreamAdapter stream = 1 [(wiresmith.options.customtype) = "github.com/grafana/loki/pkg/push.Stream"];
  repeated DroppedStream droppedStreams = 2 [(wiresmith.options.pointer) = true];
}

message SeriesRequest {
  google.protobuf.Timestamp start = 1 [(wiresmith.options.stdtime) = true];
  google.protobuf.Timestamp end = 2 [(wiresmith.options.stdtime) = true];
  repeated string groups = 3;
  repeated string shards = 4 [(wiresmith.options.jsontag) = "shards,omitempty"];
}

message SeriesResponse {
  repeated SeriesIdentifier series = 1;
}

message SeriesIdentifier {
  message LabelsEntry {
    string key = 1;
    string value = 2;
  }
  repeated LabelsEntry labels = 1;
}

message DroppedStream {
  google.protobuf.Timestamp from = 1 [(wiresmith.options.stdtime) = true];
  google.protobuf.Timestamp to = 2 [(wiresmith.options.stdtime) = true];
  string labels = 3;
}

message LabelPair {
  string name = 1;
  string value = 2;
}

// LegacyLabelPair exists for backwards compatibility reasons and is deprecated. Do not use.
// Use LabelPair instead.
message LegacyLabelPair {
  bytes name = 1;
  bytes value = 2;
}

message Chunk {
  bytes data = 1;
}

message TailersCountRequest {}

message TailersCountResponse {
  uint32 count = 1;
}

message GetChunkIDsRequest {
  string matchers = 1;
  google.protobuf.Timestamp start = 2 [(wiresmith.options.stdtime) = true];
  google.protobuf.Timestamp end = 3 [(wiresmith.options.stdtime) = true];
}

message GetChunkIDsResponse {
  repeated string chunkIDs = 1;
}

// ChunkRef contains the metadata to reference a Chunk.
// It is embedded by the Chunk type itself and used to generate the Chunk
// checksum. So it is imported to take care of the JSON representation of the
// resulting Go struct.
message ChunkRef {
  uint64 fingerprint = 1 [(wiresmith.options.jsontag) = "fingerprint"];
  string user_id = 2 [
    (wiresmith.options.customname) = "UserID",
    (wiresmith.options.jsontag) = "userID"
  ];
  int64 from = 3 [
    (wiresmith.options.jsontag) = "from",
    (wiresmith.options.casttype) = "github.com/prometheus/common/model.Time"
  ];
  int64 through = 4 [
    (wiresmith.options.jsontag) = "through",
    (wiresmith.options.casttype) = "github.com/prometheus/common/model.Time"
  ];

  // The checksum is not written to the external storage. We use crc32,
  // Castagnoli table. See http://www.evanjones.ca/crc32c.html.
  uint32 checksum = 5 [(wiresmith.options.jsontag) = "-"];
}

message ChunkRefGroup {
  repeated ChunkRef refs = 1 [
    (wiresmith.options.pointer) = true,
    (wiresmith.options.jsontag) = "refs"];
}

message LabelValuesForMetricNameRequest {
  string metric_name = 1;
  string label_name = 2;
  int64 from = 3 [
    (wiresmith.options.casttype) = "github.com/prometheus/common/model.Time"
  ];
  int64 through = 4 [
    (wiresmith.options.casttype) = "github.com/prometheus/common/model.Time"
  ];
  string matchers = 5;
}

message LabelNamesForMetricNameRequest {
  string metric_name = 1;
  int64 from = 2 [
    (wiresmith.options.casttype) = "github.com/prometheus/common/model.Time"
  ];
  int64 through = 3 [
    (wiresmith.options.casttype) = "github.com/prometheus/common/model.Time"
  ];
  string matchers = 4;
}

// TODO(owen-d): fix. This will break rollouts as soon as the internal repr is changed.
message LineFilter {
  bytes raw = 1;
}

message GetChunkRefRequest {
  int64 from = 1 [
    (wiresmith.options.casttype) = "github.com/prometheus/common/model.Time"
  ];
  int64 through = 2 [
    (wiresmith.options.casttype) = "github.com/prometheus/common/model.Time"
  ];
  string matchers = 3;
  // TODO(salvacorts): Delete this field once the weekly release is done.
  repeated LineFilter filters = 4 [
    (wiresmith.options.customtype) = "github.com/grafana/loki/v3/pkg/logql/syntax.LineFilter"
  ];
  Plan plan = 5 [
    (wiresmith.options.customtype) = "github.com/grafana/loki/v3/pkg/querier/plan.QueryPlan"
  ];
}

message GetChunkRefResponse {
  repeated ChunkRef refs = 1 [(wiresmith.options.pointer) = true];
  stats.Index stats = 2;
}

message GetSeriesRequest {
  int64 from = 1 [
    (wiresmith.options.casttype) = "github.com/prometheus/common/model.Time"
  ];
  int64 through = 2 [
    (wiresmith.options.casttype) = "github.com/prometheus/common/model.Time"
  ];
  string matchers = 3;
}

message GetSeriesResponse {
  repeated IndexSeries series = 1;
}

// Series calls to the TSDB Index
message IndexSeries {
  repeated LabelPair labels = 1 [
    (wiresmith.options.customtype) = "LabelAdapter"
  ];
}

message IndexStatsRequest {
  int64 from = 1 [
    (wiresmith.options.casttype) = "github.com/prometheus/common/model.Time"
  ];
  int64 through = 2 [
    (wiresmith.options.casttype) = "github.com/prometheus/common/model.Time"
  ];
  string matchers = 3;
  // TODO(owen-d): add shards to grpc calls so we don't have
  // to extract via labels
}

message IndexStatsResponse {
  uint64 streams = 1 [(wiresmith.options.jsontag) = "streams"];
  uint64 chunks = 2 [(wiresmith.options.jsontag) = "chunks"];
  uint64 bytes = 3 [(wiresmith.options.jsontag) = "bytes"];
  uint64 entries = 4 [(wiresmith.options.jsontag) = "entries"];
}

message VolumeRequest {
  int64 from = 1 [
    (wiresmith.options.casttype) = "github.com/prometheus/common/model.Time"
  ];
  int64 through = 2 [
    (wiresmith.options.casttype) = "github.com/prometheus/common/model.Time"
  ];
  string matchers = 3;
  int32 limit = 4;
  int64 step = 5;
  repeated string targetLabels = 6;
  string aggregateBy = 7;
  // CachingOpts frees the GetCachingOptions identifier so a hand-written
  // value getter (pkg/logproto/compat.go) can satisfy the gogo-era
  // definitions.Request interface: wiresmith message getters return
  // pointers, gogo nullable=false getters returned values.
  resultscache.CachingOptions cachingOptions = 8 [(wiresmith.options.customname) = "CachingOpts"];
}

message VolumeResponse {
  repeated Volume volumes = 1;
  int32 limit = 2;
}

message Volume {
  string name = 1 [(wiresmith.options.jsontag) = "name"];
  uint64 volume = 3 [(wiresmith.options.jsontag) = "volume"];
}

message DetectedFieldsRequest {
  google.protobuf.Timestamp start = 1 [(wiresmith.options.stdtime) = true];
  google.protobuf.Timestamp end = 2 [(wiresmith.options.stdtime) = true];
  string query = 3; // Naming this query instead of match because this should be with queryrangebase.Request interface
  uint32 lineLimit = 4;
  uint32 limit = 5;
  int64 step = 6;
  bool values = 7; // True to fetch detected field values, false for fetch detected field label names.
  string name = 8; // Name of the detected field to fetch values for.
}

message DetectedFieldsResponse {
  repeated DetectedField fields = 1 [
    (wiresmith.options.pointer) = true,
    (wiresmith.options.jsontag) = "fields,omitempty"];
  uint32 limit = 2 [(wiresmith.options.jsontag) = "limit,omitempty"];
  repeated string values = 3 [(wiresmith.options.jsontag) = "values,omitempty"];
}

// TODO: make the detected field include the serialized sketch
// we only want cardinality in the JSON response
message DetectedField {
  string label = 1;
  string type = 2 [(wiresmith.options.casttype) = "DetectedFieldType"];
  uint64 cardinality = 3;
  repeated string parsers = 4 [(wiresmith.options.jsontag) = "parsers"];
  bytes sketch = 5 [(wiresmith.options.jsontag) = "sketch,omitempty"];
  repeated string jsonPath = 6 [(wiresmith.options.jsontag) = "jsonPath,omitempty"]; // Array representing original JSON path (e.g., ["user", "id"] for field "user_id")
}

message DetectedLabelsRequest {
  google.protobuf.Timestamp start = 1 [(wiresmith.options.stdtime) = true];
  google.protobuf.Timestamp end = 2 [(wiresmith.options.stdtime) = true];
  string query = 3;
}

message DetectedLabelsResponse {
  repeated DetectedLabel detectedLabels = 1 [(wiresmith.options.pointer) = true];
}

message DetectedLabel {
  string label = 1;
  uint64 cardinality = 2;
  bytes sketch = 3 [(wiresmith.options.jsontag) = "sketch,omitempty"];
}
