{{- if .Values.ruler.remoteEvaluationDedicatedQueryPath }}
{{- $isDistributed := eq (include "loki.deployment.isDistributed" .) "true" -}}
{{- if $isDistributed -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "loki.parallelQueryFrontendFullname" . }}-headless
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "loki.parallelQueryFrontendLabels" . | nindent 4 }}
    {{- with .Values.parallelQueryFrontend.serviceLabels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
    prometheus.io/service-monitor: "false"
  annotations:
    {{- with .Values.loki.serviceAnnotations }}
    {{- toYaml . | nindent 4}}
    {{- end }}
    {{- with .Values.parallelQueryFrontend.serviceAnnotations }}
    {{- toYaml . | nindent 4}}
    {{- end }}
spec:
  clusterIP: None
  type: ClusterIP
  publishNotReadyAddresses: true
  ports:
    - name: http-metrics
      port: 3100
      targetPort: http-metrics
      protocol: TCP
    - name: grpc
      port: 9095
      targetPort: grpc
      protocol: TCP
      {{- if .Values.parallelQueryFrontend.appProtocol.grpc }}
      appProtocol: {{ .Values.parallelQueryFrontend.appProtocol.grpc }}
      {{- end }}
    - name: grpclb
      port: 9096
      targetPort: grpc
      protocol: TCP
      {{- if .Values.parallelQueryFrontend.appProtocol.grpc }}
      appProtocol: {{ .Values.parallelQueryFrontend.appProtocol.grpc }}
      {{- end }}
  selector:
    {{- include "loki.parallelQueryFrontendSelectorLabels" . | nindent 4 }}
{{- end -}}
{{- end -}}
