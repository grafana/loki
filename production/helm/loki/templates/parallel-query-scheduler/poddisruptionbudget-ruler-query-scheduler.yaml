{{- if .Values.ruler.remoteEvaluationDedicatedQueryPath }}
{{- $isDistributed := eq (include "loki.deployment.isDistributed" .) "true" -}}
{{- if and $isDistributed (gt (int .Values.parallelQueryScheduler.replicas) 1) }}
{{- if kindIs "invalid" .Values.parallelQueryScheduler.maxUnavailable }}
{{- fail "`.Values.parallelQueryScheduler.maxUnavailable` must be set when `.Values.parallelQueryScheduler.replicas` is greater than 1." }}
{{- else }}
apiVersion: {{ include "loki.pdb.apiVersion" . }}
kind: PodDisruptionBudget
metadata:
  name: {{ include "loki.parallelQuerySchedulerFullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "loki.parallelQuerySchedulerLabels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "loki.parallelQuerySchedulerSelectorLabels" . | nindent 6 }}
  {{- with .Values.parallelQueryScheduler.maxUnavailable }}
  maxUnavailable: {{ . }}
  {{- end }}
{{- end }}
{{- end }}
{{- end }}
