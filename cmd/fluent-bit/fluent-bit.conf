[INPUT]
    Name        tail
    Path        ${LOG_PATH}
[Output]
    Name loki
    Match *
    Url ${LOKI_URL}
    BatchWait 1
    BatchSize 1001024
    Labels {job="fluent-bit"}
    LineFormat json
    LogLevel info
