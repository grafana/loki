This is `a` simple span.

With `some<ht>&ml` in it.

And `` ` `` backticks.

And ``` ``some`` ``` more.

With backslash `in\` it.

This is a ` literal backtick.
As \`are\` these!

No `` literal backtick``.

`something`
